?alist
head(iris)
xx <- alist(Sepal.Length ~ dnorm(mu, sigma),)
library("rethinking")
Summary(iris)
summary(iris)
model <- rethinking::map(
alist(
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[KEY_ID] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
),
data = df
)
model <- rethinking::map(
alist(
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[KEY_ID] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
),
data = iris
)
?alist
str(iris)
model <- rethinking::map(
alist(
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
),
data = iris
)
model
rethinking::map(
alist(
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
),
data = iris
)
rethinking::map(
alist(
"Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)"
),
data = iris
)
rethinking::map(
alist(
parse(text =
"Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)"
)
),
data = iris
)
rethinking::map(
parse(text =
"alist(
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
)"
),
data = iris
)
rethinking::map(
as.expression(
"alist(
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
)"
),
data = iris
)
rethinking::map(
alist(
as.expression(
"
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
)"
)
),
data = iris
)
rethinking::map(
alist(
as.expression(
"
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
"
)
),
data = iris
)
rethinking::map(
alist(
parse(text =
"
Sepal.Length ~ dnorm(mu, sigma),
mu <- a[Species],
a[Species] ~ dnorm(5.8, 2),
sigma ~ dunif(0.01, 10)
"
)
),
data = iris
)
shiny::runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
runApp()
beaver1
ChickWeight
datasets::CO2
write.csv(datasets::CO2, path = "C:/Users/drk8b9/Desktop/rm.csv")
write.csv(datasets::CO2, "C:/Users/drk8b9/Desktop/rm.csv")
datasets::ToothGrowth
datasets::Orange
datasets::iris
write.csv(datasets::iris, "C:/Users/drk8b9/Desktop/rm.csv")
shiny::runApp()
